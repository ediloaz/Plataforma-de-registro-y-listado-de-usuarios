import { Box } from "@mui/material";
import { TopBar } from "@components/TopBar/TopBar";

export const List = () => {
  return (
    <Box sx={{ display: "flex" }} mt="2em">
      <TopBar />
    </Box>
  );
}